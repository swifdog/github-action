const swifdog = require("../../swifdog")('max@mustermann.de', 'password');

swifdog.projects.registryCredentials.list("1e0812fa-9581-4b2d-87a1-4b4e707acebf", (err, response) => {
    if (err) throw err;
    else {
        if (response.length === 0) {
            console.log('Es existieren aktuell keine Credentials in diesem Projekt.');
        } else {
            console.log('Existierende Projekte: ' + JSON.stringify(response));
        }
    }
});
